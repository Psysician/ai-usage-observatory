# CLAUDE.md

## Overview

This directory contains canonical usage-event normalization and stable event identity construction.

## Index

| File | Contents (WHAT) | Read When (WHEN) |
| ------------ | ---------------------------------------- | ------------------------------------------------------ |
| `__init__.py` | Normalization module package marker | Adjusting normalization package imports |
| `usage_event.py` | Input coercion, usage-event dataclass model, canonical field mapping, deterministic hash IDs | Modifying ingestion canonical schema or debugging parse coercion behavior |
